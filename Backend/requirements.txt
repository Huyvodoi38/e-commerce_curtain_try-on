# ============================================================
# Dependencies CORE (nhẹ) – đủ để chạy backend KHÔNG cần AI.
# Cài thêm `requirements-ai.txt` khi muốn bật tính năng Try-on.
# ============================================================

# Web framework & server
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv==1.0.1

# MongoDB (async driver) + Pydantic
motor==3.5.1
pymongo==4.8.0
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# Bảo mật & xác thực
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# Xử lý ảnh cơ bản (đọc ảnh upload, không cần GPU)
Pillow==10.4.0

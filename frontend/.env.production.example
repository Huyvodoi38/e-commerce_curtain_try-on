# Build production: cp .env.production.example .env.production && npm run build
# VITE_* được nhúng lúc build — đổi URL phải build lại

# URL API public (Render) — không slash cuối; đổi env trên Vercel rồi Redeploy
VITE_API_URL=https://api.example.com

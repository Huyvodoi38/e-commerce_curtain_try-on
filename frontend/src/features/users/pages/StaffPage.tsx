import { PageShell } from '@/components/common/PageShell'

export function StaffPage() {
  return (
    <PageShell title="Nhân viên" description="Manager only — GET/POST/PATCH /users (staff)" />
  )
}
